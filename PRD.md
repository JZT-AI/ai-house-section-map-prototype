# 365 主数据系统｜ai_house 板块全景 PRD

| 项目 | 内容 |
| --- | --- |
| 所属系统 | 365 主数据系统 → 数据管理 |
| 功能名称 | ai_house 板块全景 |
| 文档状态 | 评审稿 |
| 版本 | 1.0 |
| 更新日期 | 2026-09-17 |

## 1. 背景与目标

运营已在主数据系统维护各城市的 ai_house 板块、关联区属及电子围栏。现有管理方式以单板块维护为主，无法在同一地图中查看全城板块的分布、相邻边界和重叠范围。

在“数据管理”中新增只读的 **ai_house 板块全景** 功能。运营可按当前城市查看全部板块范围，搜索板块并同时点亮多个范围，查看板块关联区属及重叠位置，从而辅助判断板块分布与边界关系。

### 1.1 目标

- 在一张真实地图中展示当前城市 ai_house 板块的电子围栏、名称和关联区属。
- 支持名称搜索联想、默认多选点亮、定位全部已选板块。
- 支持识别同一点击位置命中的多个板块，不对重叠关系做归属判断。
- 围栏新增、修改、删除或关联区属变更后，重新进入或刷新页面可查看最新数据。

### 1.2 非目标

- 不在本页面新增、编辑、删除或绘制板块和电子围栏。
- 不计算重叠面积、不判定板块归属、不自动修正边界。
- 不建立新的板块、区属或围栏数据对象；沿用现有主数据。

## 2. 用户与入口

| 项目 | 需求 |
| --- | --- |
| 使用角色 | 有 ai_house 板块查看权限的运营人员、数据管理员。 |
| 菜单入口 | 主数据系统 → 数据管理 → ai_house 板块全景；与“ai_house 板块管理”同级。 |
| 城市范围 | 沿用主数据系统当前城市及城市权限，不单独维护城市配置。 |
| 页面属性 | 只读。板块或围栏问题通过现有“ai_house 板块管理”处理。 |

## 3. 页面结构

| 区域 | 功能 |
| --- | --- |
| 页面标题与工具栏 | 展示功能名称；提供“使用说明”和“刷新数据”。 |
| 左侧检索列表 | 展示当前城市板块；支持名称搜索联想、全部板块与未展示板块切换。每条记录展示板块名称及关联区属名称。 |
| 地图区域 | 展示高德真实底图、板块电子围栏、板块名称、缩放控件和查看全城入口。 |
| 已选信息卡 | 展示已点亮板块数量及名称；支持“定位全部”和“取消选择”。 |
| 重叠候选面板 | 点击重叠位置时展示全部命中板块，可加入或移出已点亮集合。 |

## 4. 功能需求

### 4.1 城市切换与数据加载

1. 页面打开时，按系统当前城市加载有查看权限的 ai_house 板块。
2. 切换全局城市时，清空搜索、已点亮板块和重叠候选，加载新城市数据并适配地图全城范围。
3. 地图、左侧列表、搜索联想和已选信息必须使用同一批城市数据。
4. 点击“刷新数据”后重新读取当前城市已保存的数据；刷新期间显示加载状态，完成后重新绘制地图与列表。
5. 当前城市没有可展示围栏时，保留城市底图并提示“当前城市暂无可展示板块”。

### 4.2 板块列表与关联区属

1. 左侧列表展示当前城市全部 ai_house 板块，不受原管理列表分页影响。
2. 每条列表记录展示：
   - 板块名称；
   - 关联区属名称，多个区属按既有关联顺序以“·”分隔；
   - 未关联区属时展示“关联区属：暂未配置”。
3. 关联区属从现有 ai_house 板块与区属的关联数据读取，不以名称推导或在全景页单独维护。
4. 同名板块按板块 ID 分别展示和选择，不因名称相同而合并。
5. 已点亮的板块在列表中保持高亮；多个板块可同时高亮。

### 4.3 地图展示

1. 地图使用高德真实底图，支持拖动、滚轮缩放、双击缩放、键盘缩放和查看全城。
2. 使用当前保存的电子围栏绘制板块范围；围栏与底图在同一地图中展示。
3. 未选中围栏使用可辨识的中等透明填充和清晰描边；已点亮围栏使用更粗的蓝色描边、更高填充不透明度和更高层级。
4. 板块名称始终展示，不提供隐藏或取消展示的操作。名称标签应定位在电子围栏内部的视觉中心，不得固定使用围栏首个坐标点；密集区域允许避让部分非选中标签。
5. “查看全城”清空搜索和已选状态，并适配当前城市全部可展示围栏范围。

### 4.4 搜索联想

1. 支持按当前城市板块名称进行包含匹配，忽略首尾空格及英文字母大小写。
2. 输入关键词后展示匹配的联想结果，同时更新左侧列表；地图保留全城围栏。
3. 联想结果展示板块名称及关联区属名称；同名记录分别展示。
4. 点击联想结果或列表记录时，将板块加入已点亮集合；若该板块已点亮，再次点击则取消该板块的点亮状态。
5. 只有一个匹配结果时，按 Enter 可直接加入已点亮集合；多个匹配结果时提示选择具体板块。
6. 无匹配结果时提示“没有匹配的板块，请更换关键词”，不清空地图。

### 4.5 默认多选点亮

1. 板块点亮默认为多选，不提供开启或关闭多选的配置。
2. 从列表、搜索联想、地图单点命中或重叠候选选择板块时，均可加入或移出已点亮集合。
3. 首次点亮时，地图适配该板块完整范围；后续点亮保持当前地图视野。
4. 点击“定位全部”后，地图适配所有已点亮板块范围，并预留右侧信息卡空间。
5. 点击空白地图区域不清空已点亮板块；运营通过“取消选择”或“查看全城”清空已点亮集合。

### 4.6 重叠与边界命中

1. 点击地图时计算该位置命中的全部可展示板块，不只返回最上层围栏。
2. 单个命中时，直接加入或移出该板块的点亮状态。
3. 多个命中时，展示“点击位置命中 N 个板块”候选列表；运营可逐条加入或移出已点亮集合。
4. 共边、包含或完全重叠的多个板块均按多候选处理。
5. 本功能只展示命中关系，不判定板块归属，也不标记重叠为异常。

### 4.7 异常与空状态

| 场景 | 系统行为 |
| --- | --- |
| 板块未配置电子围栏 | 列表可检索，展示未展示原因；地图不绘制虚假范围。 |
| 电子围栏无法解析 | 列表归入“未展示”，展示原因；不阻塞其他板块展示。 |
| 区属未关联 | 列表展示“关联区属：暂未配置”。 |
| 地图底图加载失败 | 展示明确失败提示和重试入口；已加载的板块列表保留。 |
| 板块数据加载失败 | 展示失败状态和重试入口；不展示旧城市数据。 |

## 5. 数据与权限

### 5.1 数据要求

| 数据 | 使用方式 |
| --- | --- |
| 板块 ID | 列表、搜索、点亮状态、重叠候选及刷新后的稳定识别。 |
| 板块名称 | 列表、地图标签、搜索联想与已选信息展示。 |
| 城市 | 按系统当前城市及权限过滤。 |
| 关联区属 | 列表与搜索联想展示；读取既有板块—区属关联。 |
| 电子围栏 | 地图绘制、点击命中、定位范围。 |
| 板块有效状态 | 沿用既有主数据状态，过滤不可用或已删除记录。 |

### 5.2 高德地图接口要求

| 接口/对象 | 用途 | 本功能使用规则 |
| --- | --- | --- |
| 地图 JS API 2.0 / `AMap.Map` | 加载真实底图及承载覆盖物 | 创建当前城市的地图实例，开启拖动、滚轮缩放、双击缩放与键盘缩放。 |
| `AMap.Polygon` | 绘制电子围栏 | 每个可展示板块对应一个或多个面覆盖物；覆盖物携带板块 ID，用于点击命中、样式切换和定位。 |
| `AMap.Text` | 展示板块名称 | 在围栏内部视觉中心展示名称；已点亮板块的标签置顶。 |
| `AMap.Scale` | 展示地图比例尺 | 地图加载后显示比例尺。 |
| `map.setFitView(overlays, ..., avoid)` | 定位范围 | 首次点亮时适配单个板块；点击“定位全部”时适配已点亮板块；通过 `avoid` 预留右侧信息卡空间。 |

1. 接入使用高德 **Web 端（JS API）Key** 和地图 JS API 2.0；地图加载方式统一使用官方 Loader 或在线 JS API 脚本，禁止将高德 JS API 文件下载后与业务代码混合打包。
2. Key 在 2021-12-02 后创建时，须同时配置安全密钥。生产环境通过服务端代理配置 `window._AMapSecurityConfig.serviceHost`，并且必须在加载地图 JS API 前完成配置；不得在前端代码、接口响应、日志或公开仓库中写入安全密钥。
3. Key 的域名白名单须包含实际部署域名。域名、Key 标识及安全密钥保管方式见《[ai_house 板块全景-高德密钥配置说明](ai_house板块全景-高德密钥配置说明.md)》。
4. 板块搜索只查询主数据，不调用高德 POI 搜索或输入提示接口。

高德接口说明以官方文档为准：[JS API 加载](https://lbs.amap.com/api/javascript-api-v2/guide/abc/load)、[多边形覆盖物](https://lbs.amap.com/api/javascript-api-v2/guide/amap-polygon/polygon)、[视野适配](https://lbs.amap.com/api/javascript-api-v2/guide/map/state)、[安全密钥使用](https://lbs.amap.com/api/javascript-api-v2/guide/abc/jscode)。

### 5.3 权限要求

| 角色能力 | 规则 |
| --- | --- |
| 查看全景 | 需具备当前城市 ai_house 板块查看权限。 |
| 查看区属及围栏 | 与板块查看权限一致，后端按城市权限过滤。 |
| 编辑板块或围栏 | 本页不提供编辑能力；沿用原板块管理权限和入口。 |

## 6. 验收标准

| 编号 | 验收场景 | 预期结果 |
| --- | --- | --- |
| A01 | 进入全景页 | 当前城市有权限的 ai_house 板块、关联区属和电子围栏正确加载；地图适配全城范围。 |
| A02 | 切换城市 | 搜索、已点亮状态和候选清空；地图与列表仅展示新城市数据。 |
| A03 | 查看左侧列表 | 每条记录的名称下展示全部关联区属；无关联时展示“暂未配置”。 |
| A04 | 搜索名称并选择结果 | 正确展示联想；选择结果加入已点亮集合，地图与列表同步高亮。 |
| A05 | 连续选择两个板块 | 无额外配置时两个围栏同时点亮，列表同时高亮；点击“定位全部”可看到两个完整范围。 |
| A06 | 再次选择已点亮板块 | 仅移出该板块，其他已点亮板块保持不变。 |
| A07 | 点击单板块、重叠位置、共边位置和空白位置 | 分别支持加入/移出单板块、展示全部命中候选、展示全部命中候选、保留现有已点亮状态。 |
| A08 | 查看名称位置与地图缩放 | 名称位于各自围栏内部；未选中范围可辨识，底图道路和 POI 可见。 |
| A09 | 无围栏、区属未关联、底图或数据加载失败 | 分别展示对应原因或失败反馈，不影响其他正常板块查看。 |
| A10 | 在原板块管理页修改围栏或关联区属后刷新全景页 | 地图边界、名称和列表区属展示为最新保存结果。 |

## 7. 变更记录

| 日期 | 版本 | 说明 |
| --- | --- | --- |
| 2026-09-17 | 1.2 | 补充高德地图 JS API 2.0 的接口使用范围、安全密钥及域名白名单要求。 |
| 2026-09-17 | 1.1 | 板块名称调整为始终展示，移除名称显示操作。 |
| 2026-09-17 | 1.0 | 首次整理为主数据系统 ai_house 板块全景功能需求。 |
