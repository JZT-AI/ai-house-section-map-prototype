# 主数据系统 · ai_house 板块全景 PRD

| 项目 | 内容 |
| --- | --- |
| 所属系统 | 365 主数据系统 → 数据管理 |
| 功能名称 | ai_house 板块全景 |
| 文档状态 | 评审稿；核心交互已通过本地原型验证，生产接口与坐标契约待联调核对 |
| 更新日期 | 2026-09-16 |
| 关联原型 | [打开 HTML 交互原型](主数据系统-ai_house板块全景-交互原型.html) |
| 本期范围 | 同城板块全景、搜索联想与点亮、重叠点选择、异常提示、打开及刷新时读取最新数据 |

## 1. 背景与目标

主数据系统已有各城市 `ai_house` 板块及电子围栏，运营在现有板块管理中生产、编辑。本次提供的截图显示：当前通过列表逐条打开围栏编辑弹窗，难以同时判断全城板块分布、相邻边界及重叠关系。

本期为运营新增一个只读全景查看入口：把当前城市的可展示围栏叠加到同一张地图，支持搜索定位和重叠点多板块选择。运营能在同一页完成“看全城 → 找板块 → 看边界 → 辨认交叠”，无需反复打开多个围栏编辑弹窗。

完成标准是：当前城市应展示的有效范围无遗漏，名称及边界与主数据一致；目标板块可以被准确检索和点亮；重叠处的每个命中板块都能选择；源数据更新后重新进入或刷新即可查看。

### 1.1 依据及优先关系

| 依据 | 本期采用内容 |
| --- | --- |
| 用户本次明确要求 | 功能放在“主数据系统 → 数据管理”；增加搜索联想、检索后点亮；考虑板块边界可能重叠。 |
| 图 1、图 2 | 沿用现有系统导航、城市上下文、板块及围栏维护路径。已有围栏编辑和保存能力，本页不重复建设。 |
| 图 3《板块全景地图需求》 | 同城叠加、名称及边界、颜色区分、缩放拖动、打开或刷新后同步、只读、无围栏及异常提示。原文“暂不要求固定入口”被本次用户明确要求覆盖。 |
| [飞书 ai_house 区属板块清单](https://ax047hrq4li.feishu.cn/wiki/NxFSwa0Msi9KxJkQOfLcr7VAnMd?from=from_copylink&sheet=9enKWM) | 本次核对了合肥及西安页，作为命名和区属关系的参考；不直接作为生产围栏数据源。指定链接对应合肥。 |
| [真实围栏导出 Excel](master_data_service_hd_ai_house_section.xlsx) | HTML 原型的真实范围来源。字段为 `name`、`city`、`region_range`；采用原始坐标，未改写源表。 |
| [既有西安板块需求](主数据系统新增西安ai_house区属板块-需求.md) | 复用既有 `ai_house` 板块对象和电子围栏维护方式。 |

### 1.2 数据核对发现

围栏源表 `Result 1` 共 486 条数据。`Query` 页中的查询为：

```sql
select name, city, region_range
from hd_ai_house_section
where region_range is not null
```

因此，这份文件**只证明导出的非空围栏记录**，不包含无围栏全量，也没有板块有效状态、真实板块 ID、区属、中心坐标和修改时间。不能据此宣称“站内总共 486 个有效板块”或“无围栏 0 个”。

| 城市 | 源表记录数 | 原型已绘制 | 边界自相交待核对 |
| --- | ---: | ---: | ---: |
| 合肥 hf | 81 | 80 | 1 |
| 南京 nj | 134 | 125 | 9 |
| 无锡 wx | 72 | 68 | 4 |
| 芜湖 wh | 50 | 48 | 2 |
| 苏州 sz | 98 | 95 | 3 |
| 西安 xa | 51 | 50 | 1 |
| 合计 | 486 | 466 | 20 |

检查结果：全部 486 条可解析为经纬度点数组，本次数据均为单个外层点环；经纬度可转换为有限数值且未越界。按非相邻线段严格交叉检查，20 条存在自相交，原型将其列入“未展示”，没有自动修正坐标。该检查不等于已经完成源系统全部几何规则验收。

飞书合肥页有 89 行板块记录，围栏快照合肥有 81 条；飞书西安页有 87 行，快照西安有 51 条。二者记录范围、时间及有效性口径不能仅凭本次材料判断，**差值不能直接视为无围栏数量**。合肥快照中两个“方兴”分别来自源表第 5、55 行，原型分别保留。

## 2. 本期范围与复用原则

- 在数据管理下新增 **ai_house 板块全景** 菜单，与现有 ai_house 板块管理相邻。新增页面的必要性是同时承载全城围栏和交叠查看，现有逐条编辑弹窗无法清晰完成该任务。
- 复用既有板块 ID、名称、城市、区属及围栏；复用现有城市切换和地图服务。不新建“全景板块”对象，不单独维护一套边界，不设置手工地图生成或同步任务。
- 当前城市仅展示 `ai_house` 板块；不混入新房板块、存量房板块或另行推导的行政区边界。
- 本页只读，不提供新增、编辑、删除、绘制、修边、合并、审核或保存围栏。运营发现问题后沿现有 ai_house 板块管理路径处理。
- 不做重叠面积计算、全城重叠巡检报告、归属优先级判定、空白区补齐或房源重新归属。重叠本身不是异常，也不阻止围栏展示。
- 不做自动实时推送或定时刷新；保证重新进入、浏览器刷新和页面“刷新数据”可读取最新已保存数据。

## 3. 页面与运营路径

### 3.1 页面组成

延续当前主数据系统样式。顶部保留全局城市切换；左侧沿用数据管理菜单；主内容为检索列表和地图。

| 区域 | 内容及用途 |
| --- | --- |
| 标题及工具 | ai_house 板块全景、只读说明、使用说明、刷新数据。 |
| 检索列表 | 板块名称搜索联想；“全部板块”“未展示”两个视图；记录名称和区分标识；当前匹配数量。列表可滚动。 |
| 全景地图 | 当前城市全部可展示围栏、名称、半透明填充和边界；拖动、缩放、查看全城、名称显示开关。 |
| 选中信息 | 当前板块名称、城市、板块 ID、关联区属（接口具备时）、展示状态；定位板块、取消选中。 |
| 重叠候选 | 点击位置命中的全部板块，逐条可选；选中状态与地图一致。 |
| 未展示提示 | 地图角落显示数量，点击切换到未展示列表；选中记录可见具体原因。 |

名称、ID、区属承担识别作用，不新增与查看无关的经营统计卡片。地图标签过密时按缩放层级避让，列表始终提供全部记录，选中板块名称优先显示。

### 3.2 主流程

1. 运营进入全景页，沿用全局当前城市和授权范围，自动加载该城市数据并适配全城视野。
2. 查看全城分布；需要找某板块时输入名称，从联想结果或列表选择具体记录。
3. 地图定位到该板块完整围栏，边界加粗并点亮，显示板块信息。
4. 需要辨认交叠位置时点击地图。若有多个命中，逐个选择候选，观察完整边界。
5. 发现无围栏或异常时查看原因，在既有板块管理中处理；返回全景后刷新即可查看最新状态。

## 4. 功能规则

### 4.1 城市与加载

- 默认使用主数据系统当前城市，不在全景页另建独立城市配置。切换城市时，清空搜索、选中态、重叠候选和旧城地图，加载新城完整记录并适配新城范围。
- 首次加载显示加载中。数据返回后，地图和列表作为同一批结果更新；禁止旧城市数据闪回或覆盖新城市结果。
- 正式页面的数据总数指“当前城市、当前用户可见、按既有有效口径可用的 ai_house 板块数”，包含无围栏和异常记录。已展示数按板块计数，一个板块多片范围仍计 1 条；总数 = 已展示 + 未展示。
- 读取数据不能受管理列表当前分页限制。若接口分页，须取齐后展示，或在全部完成前明确显示加载中，不把第一页作为全量。
- “有效”沿用既有可用、删除等状态定义；不得因本期缺少说明新增一个业务有效状态。

### 4.2 地图绘制与查看

- 地图底图采用**高德地图 JS API 2.0** 的 `AMap.Map`。道路、河流、POI、底图标注与电子围栏处在同一地图实例中；不再以经纬度网格或静态图片替代真实地图。
- 围栏采用 `AMap.Polygon` 覆盖物渲染，保存板块 ID 到覆盖物 `extData`。当前选中板块提高描边宽度、填充不透明度和层级；其他围栏半透明叠加，使道路、POI 和底图仍可辨认。
- 复用现有电子围栏编辑页的高德地图配置、坐标系、坐标转换和围栏解析规则。不能通过截图推定坐标系，也不能默认把 Excel 的 latitude/longitude 当作 WGS84。若现有围栏不是高德 GCJ-02，必须在渲染前使用既有转换链路统一到高德坐标。
- 按保存的真实围栏绘制，不对边界做裁切、自动修边或合并。存在重叠、包含关系时保留每条独立范围。
- 默认适配当前城市全部可展示围栏的包围范围；没有可绘制围栏时，保留城市底图并显示原因，不跳至其他城市或世界视图。
- 用半透明填充及清晰描边区分板块，相邻板块优先使用不同颜色；颜色只用于视觉区分，不代表行政区、状态或业务等级。显示不能仅依赖颜色，须配合名称、描边和选中态。
- 开启高德原生 `dragEnable`、`scrollWheel`、`doubleClickZoom`、`keyboardEnable` 和移动端 `touchZoom`。工具栏的放大、缩小、比例尺与“查看全城”调用当前 `AMap.Map` 实例；“查看全城”清空搜索与选中状态，恢复全部列表并通过 `map.setFitView` 适配全城。
- 从列表或搜索定位板块时，以该板块的 `AMap.Polygon` 调用 `map.setFitView` 并预留右侧详情卡空间；禁止只移动中心点导致范围被遮挡。高德官方文档已明确 `setFitView` 可根据覆盖物自动适配视野。
- 名称默认显示。全城视野密集处允许避让部分标签，放大后补充显示；全部名称可通过列表和联想访问。选中板块的标签优先显示，不能被普通标签遮盖。
- 如果既有数据支持多个外环、洞或其他图形类型，应按既有结构完整显示，不只取第一个环，也不把洞误绘为覆盖范围。本次 Excel 均为单环，其他类型需使用生产样例联调验证。

### 4.3 搜索联想

- 按**当前城市全部有效板块名称**做包含匹配，忽略首尾空格及英文字母大小写。不要求拼音、地址或自然语言搜索。
- 输入后展示联想列表，同时更新左侧匹配记录数和列表；地图仍保留全城围栏，输入动作本身不自动选中或隐藏其他板块。
- 联想项显示板块名称、区属和板块 ID；区属存在多值时完整提供，名称相同的记录分别列出。若区属字段暂未提供，以城市及真实 ID 区分，不能伪造或猜测关系。
- 鼠标选择联想结果或列表记录即可点亮；键盘上下键移动候选，Enter 选中，Esc 关闭联想。无键盘高亮时按 Enter：只有一个匹配结果才直接定位；有多个结果则提示选择，不自动取第一条。
- 没有匹配时提示“没有匹配的板块，请更换关键词”，保留地图；清空搜索恢复全部记录和全城视野，并清除选中。
- 无围栏、异常板块仍须可检索。选择后展示原因，不移动地图到假坐标，不显示为正常已点亮。
- 搜索请求若异步返回，以最新输入为准；旧结果不能覆盖新关键词。

### 4.4 选中与点亮

- 当前只选中一个板块。选中后目标范围填充加深、边界加粗并置顶，其他板块降低强调程度但继续显示。
- 从搜索或列表选中时，视野适配目标的全部有效范围，预留信息卡空间；不能只把中心坐标移入屏幕而截断边界。
- 从地图单点或重叠候选选择时保持当前视野，避免切换候选导致上下文跳动。需要整体查看时可通过该记录的定位入口定位。
- 重叠候选保留在同一位置，可连续切换不同板块观察。取消选中、关闭信息卡或点击空白处恢复普通样式；取消选中不强制改变缩放和位置。
- 选中基于稳定板块 ID，不基于名称。名称修改不产生新对象；同名不同 ID 不得自动合并。

### 4.5 重叠与边界命中

- 点击地图时计算该位置命中的**所有可展示板块**，不能只取地图图层最上层的对象。
- 无命中：清除选中和候选。单个命中：直接点亮该板块并展示信息。多个命中：显示完整候选列表，由运营选择。
- 同一板块多片范围同时命中时，按板块 ID 去重；不同板块即使名称或几何范围相同也保留。
- 共边位置可能命中多个板块，也进入候选选择。不能将“该点命中多个板块”直接写成“面积重叠异常”；候选提示使用“点击位置命中 N 个板块”。
- 候选按与列表一致的稳定顺序展示，支持滚动，不以图层层级或名称作为优先归属判断。
- 原型已用真实坐标验证合肥“包公 / 芜湖路”等交叠示例。正式验收还需包含小板块被大板块完全覆盖的场景，确保下层板块仍可选。

### 4.6 无围栏、异常及服务失败

| 场景 | 页面行为 |
| --- | --- |
| 无电子围栏 | 不绘制；保留在全部及未展示列表，可搜索；提示“未配置电子围栏”。 |
| 坐标无法解析、非数值、越界、无法形成有效面积或几何自相交 | 暂不绘制该板块，保留名称、ID和具体原因；正常板块继续绘制；不得自动修正后当作有效范围。 |
| 一条板块的部分范围异常 | 本期默认整条板块未展示，防止部分绘制造成“范围完整”的误解；说明异常原因。复用既有更严格规则时须保持完整性提示。 |
| 合法重叠或包含 | 正常绘制，通过候选选择区分，不归入未展示。 |
| 关联区属缺失 | 若名称、城市、ID、围栏仍可正常识别，不因此拒绝绘制；信息处提示未提供。 |
| 城市暂无有效板块 | 显示“当前城市暂无板块”，允许切换城市或刷新。 |
| 城市全部未配置或全部异常 | 显示列表和未展示原因；地图显示“当前城市暂无可展示围栏”，不伪装为数据加载失败。 |
| 接口失败或超时 | 显示加载失败和重试；不把旧图伪装成最新数据。本期采用遮罩阻止继续操作旧数据，重试成功后整体替换。 |
| 地图底图服务失败 | 明确提示底图加载失败并提供重试。若围栏图层已成功加载，可保留边界和列表，但提示地理参照不可用。 |
| 高德 Key、安全密钥、域名白名单或安全代理校验失败 | 提示“高德地图加载失败，请核对 Key、安全密钥、域名白名单和网络”；不把离线/旧底图伪装为真实地图。 |
| 无城市权限或无功能权限 | 沿用系统鉴权，提示无权访问；不能仅隐藏菜单而仍返回该城市数据。 |

原型的自相交检查只用于暴露本次样本风险；上线复用或补齐生产几何校验，不能把“能被浏览器画出来”作为有效性的唯一标准。

### 4.7 数据同步

- 打开页面、重新进入功能、浏览器刷新、点击“刷新数据”时重新读取当前已保存数据；不要求运营另行生成地图或手动同步。
- 覆盖新增、删除或失效、改名、城市归属变更、围栏新增或修改、围栏清空。旧范围必须被移除，不能只追加新范围。
- 刷新后：仍存在且可绘制的选中 ID 保持选中，并适配更新后的完整范围；已删除或不可访问则清除选中并提示；已无围栏或异常则停止高亮并显示原因。
- 刷新重新执行当前搜索条件；清除旧的重叠候选，因为命中结果可能变化。
- 连续刷新避免重复并发操作；切换城市或新刷新发起后，废弃旧请求结果。缓存若存在，须保证重新打开及刷新可读到保存成功的数据。
- 页面无自动推送时不承诺实时变化。生产页面显示“本次加载时间”即可，不将加载时间当成板块修改时间。

## 5. 数据与权限契约

### 5.1 复用字段

以下是应具备的语义，不规定新建数据库字段或新建接口；优先通过现有查询能力补齐返回内容。

| 数据 | 使用方式 |
| --- | --- |
| 板块 ID | 稳定唯一标识，支撑同名区分、列表及图层联动、刷新保留选中。生产不可使用名称或临时行号替代。 |
| 城市标识 | 过滤当前授权城市，沿用系统城市字典。 |
| 板块名称 | 标签、列表和联想展示，以主数据当前值为准。 |
| 关联 ai_house 区属 | 搜索消歧及选中详情；多值完整展示；复用已有关系。 |
| 电子围栏 | 复用现有结构及坐标语义；维持源精度，保留全部范围。 |
| 既有有效性信息 | 沿用生产状态筛选；由后端过滤或明确返回，不增设全景专属状态。 |
| 加载时间 | 页面读取成功时记录，仅说明本页快照时间。 |

“已展示”“未展示”和具体原因均为本页派生结果，不新建生产任务、工单或生命周期状态。异常日志记录城市、板块 ID 和失败原因，便于沿现有路径核对。

### 5.2 权限

沿用主数据系统账号、城市权限及菜单权限机制。本功能查看权限与编辑能力分离：可查看全景不意味着可编辑围栏。已有菜单权限机制若要求登记新菜单权限，按既有机制登记，不另建角色或授权体系。所有生产数据读取须执行后端权限校验。

## 6. 验收用例

共通前置：已登录且有目标城市查看权限，测试环境可获取该城市完整板块及围栏；以同一时点主数据查询结果为对照。

| 编号 | 操作或前置 | 应有结果 |
| --- | --- | --- |
| A01 | 首次打开；数据量超过管理列表一页 | 全部有效板块进入列表，可绘制范围全部叠加；统计对账一致，无分页遗漏。 |
| A02 | 切换城市并快速来回切换 | 清空旧城检索及选中，地图只出现最终城市数据，无旧请求覆盖。 |
| A03 | 输入部分名称、首尾空格、英文大小写变化 | 正确联想当前城市板块；未选择时地图保留全城。 |
| A04 | 同名不同 ID、多区属；直接 Enter 及上下键选择 | 同名记录分别可见；无高亮且多结果时不自动选第一条；键盘可选择具体记录。 |
| A05 | 从联想或列表选择板块 | 唯一记录点亮并置顶；完整范围位于可视区；其余板块仍可见。 |
| A06 | 点击真实重叠位置、完全包含位置、完全相同的不同板块范围 | 全部命中 ID 可见并可逐一切换；不因遮挡丢失下层板块，不按名称合并。 |
| A07 | 点击共边、单板块、空白区域 | 分别进入多候选、单选、取消选中；共边不自动判为重叠错误。 |
| A08 | 拖动、缩放、隐藏名称、查看全城 | 边界与位置一致；名称避让；查看全城恢复列表及完整范围。 |
| A09 | 检索无围栏、异常板块 | 可检索，可看到原因；地图不绘制假范围；其他正常板块不受影响。 |
| A10 | 空城市、全部无围栏、全部异常、加载失败、底图失败 | 分别出现对应状态和可用恢复操作，不混淆空数据与服务失败。 |
| A11 | 在原管理页新增、改名、改围栏后刷新 | 最新数据出现；选中按 ID 保持，视野适配新范围；旧边界移除。 |
| A12 | 删除、失效、跨城移动或清空已选中板块的围栏，再刷新 | 旧范围与旧候选消失；选中被清除或转为未展示，并说明原因。 |
| A13 | 请求失败后重试；重复刷新和切城 | 不把旧数据当作最新；成功恢复；无城市串数据。 |
| A14 | 无功能权限、无城市权限、只有查看权限 | 按权限拒绝数据或允许查看；本页不能编辑或保存围栏。 |
| A15 | 对比现有围栏编辑地图，含多片范围和洞的测试数据 | 同一地图参照下边界一致，坐标无偏移，不遗漏外环或填平洞。 |

性能验证使用目标城市真实全量及最大点数样例。验收观察加载、拖动、缩放和联想是否可连续操作；不以 486 条本地快照的结果代替生产接口性能承诺。发现明显卡顿时先优化渲染和数据读取，不通过静默截断板块数量解决。

上线前由研发核对第 8 节的数据契约，测试覆盖上述用例，运营对比现有管理页确认名称及边界。上线后按城市抽查刷新一致性；全景问题不应影响原板块维护，可沿现有发布机制回退全景入口和实现。

## 7. HTML 原型说明与验证边界

原型位于现有“主数据系统”目录。该目录尚无独立产品版本约定，本次沿用目录和名称，不虚构产品发布版本。文档修订记在文末。

- 单文件 HTML，内嵌本次 Excel 的原始围栏坐标；未配置高德 Key 时保留离线 SVG 预览。配置 Web端（JS API）Key 后，动态加载高德 JS API 2.0，真实道路、POI、底图与 `AMap.Polygon` 围栏在同一地图实例中展示。
- 支持 6 个城市、486 条记录；466 条绘制，20 条自相交记录保留在未展示列表。默认合肥；城市切换可查看西安等数据。
- 实现名称联想、同名记录分别选择、完整范围定位、单点全部命中、候选切换、拖动缩放、名称显示、全城恢复及异常详情。
- 页底“原型演示”可进入无围栏、空城市、加载失败场景，也可定位每城已核验的真实交叠点。模拟状态明确标注，未改写 Excel；原型演示控件不属于正式产品范围。
- 原型增加“配置高德真实底图”入口。Key 与安全密钥仅保存于当前浏览器的 localStorage，不写入 HTML 或仓库；没有提供真实 Key 时，无法从本地替你验证高德瓦片、POI 与边界的实际对齐。源表没有坐标系标记，因此即使成功加载底图，仍须使用现有围栏编辑页抽样完成坐标对齐验收。
- 源表没有真实业务 ID，原型用 Excel 行号区分，不虚构板块 ID；没有把飞书区属按名称强行合并进真实围栏记录。正式页面使用主数据 ID 和对应关系。
- 刷新按钮仅重新读取内嵌快照并演示加载反馈，明确提示不会连接生产系统。HTML 不承担实时同步；本 PRD 的打开及刷新更新规则是待开发的生产要求。
- 已在隔离浏览器验证城市记录数、绘制数、同名联想、键盘选择、重叠候选切换、异常提示、空结果、模拟无围栏、空城市、失败重试和刷新。检查桌面及窄屏布局；这些属于原型运行验证，不是生产联调验收。

### 7.1 本次源数据的自相交记录

共同原因：检测到非相邻线段严格交叉，原型未绘制。行号为 Excel `Result 1` 中的实际行号，仅作核对定位；源记录没有被修改。

| 城市 | 板块 | 源表行号 |
| --- | --- | ---: |
| 合肥 | 莲花 | 58 |
| 南京 | 江北中央商务区 | 147 |
| 无锡 | 黄巷 | 216 |
| 无锡 | 刘潭 | 221 |
| 无锡 | 鹅湖 | 249 |
| 无锡 | 南闸街道 | 260 |
| 芜湖 | 南瑞新城 | 273 |
| 芜湖 | 无为县 | 319 |
| 苏州 | 汾湖 | 334 |
| 苏州 | 横扇 | 340 |
| 苏州 | 张浦 | 413 |
| 南京 | 大校场 | 419 |
| 南京 | 苜蓿园 | 422 |
| 南京 | 江宁大学城 | 424 |
| 南京 | 秣陵 | 425 |
| 南京 | 葛塘大厂 | 426 |
| 南京 | 汤泉街道 | 431 |
| 南京 | 雨核 | 434 |
| 南京 | 兴智 | 436 |
| 西安 | 城东 | 447 |

## 8. 开发前需核对的现有契约

这些是生产接入前的核对项，不阻塞本次原型交付；优先查现有代码、接口及数据库，不要求运营另行定义技术实现。

| 核对项 | 当前证据及处理原则 |
| --- | --- |
| 稳定板块 ID、区属关系与有效记录口径 | 导出未提供，须从现有主数据查询获取。按 ID 关联，同名不自动合并；查询包含无围栏记录。 |
| 高德 JS API 2.0 配置 | 使用 Web端（JS API）Key。2021-12-02 后创建的 Key 还需安全密钥；生产按高德推荐的安全代理接入，Key 设域名白名单，安全密钥留在服务端，不出现在页面、日志或仓库。 |
| 地图供应商、坐标系和图形结构 | Excel 只有 latitude/longitude，无法判定坐标系。直接复用现有高德编辑页的解析和转换，取样对齐后上线。 |
| 几何异常规则 | 原型检查发现 20 条自相交。核对现有解析是否存在特别语义；确认确为异常时沿原维护流程修正，本页不自动修复。 |
| 实际可用城市及权限 | 原型包含源表的 6 城市；生产由系统授权城市和当前数据决定，不将样本城市硬编码为永久范围。 |
| 接口全量能力、缓存与最新读取 | 核对是否分页、是否排除空围栏、缓存何时失效。保证重新进入及刷新可看到保存成功的数据，不新增人工同步步骤。 |

## 变更记录

| 日期 | 文档修订 | 内容 |
| --- | --- | --- |
| 2026-09-17 | 1.1 | 明确采用高德地图 JS API 2.0 真实底图与 `AMap.Polygon` 覆盖物；补充 Key、安全密钥、安全代理、原生缩放及地图对齐验收要求；原型新增可配置的高德真实地图模式。 |
| 2026-09-16 | 1.0 | 按用户指定的主数据入口形成 PRD；补齐搜索点亮、同名消歧、重叠点候选；接入真实围栏导出，记录自相交异常、快照覆盖限制及生产接入要求。 |
